import {StyleSheet} from 'react-native';

const s = StyleSheet.create({
  errorWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  iconWrapper: {
    width: 24,
    height: 24,
    marginRight: 8,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textWrapper: {
    marginVertical: 6,
  },
});

export default s;
