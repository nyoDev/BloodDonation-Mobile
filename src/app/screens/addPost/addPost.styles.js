import {StyleSheet} from 'react-native';
import {Colors} from 'theme/index';

const s = StyleSheet.create({
  formContainer: {
    marginTop: 15,
  },
  bottomSection: {
    marginHorizontal: 18,
    paddingVertical:10
  },
});

export default s;
